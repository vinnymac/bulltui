# Runs the workspace unit tests on every push/PR to main.
when:
  - event: ["push", "pull_request"]
    branch: ["main"]

engine: microvm

dependencies:
  - rustc
  - cargo
  - gcc

environment:
  CARGO_TERM_COLOR: always

steps:
  - name: unit tests
    command: cargo test --workspace --lib
