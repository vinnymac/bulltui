# bulltui

## 0.2.0

### Minor Changes

- Switch npm wrapper to ESM-only — drop the `'use strict'` / `require()` wrapper and emit a native ES module (`"type": "module"` + `import` syntax). Node ≥ 18 is already required; ESM has been the right default since Node 12.

## 0.1.1

### Patch Changes

- Release docker ghcr image
